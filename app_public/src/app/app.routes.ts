import { Routes } from '@angular/router';
import { HomeListComponent } from './home-list/home-list.component';

export const routes: Routes = [
    { path: '',   title:'Home', component:HomeListComponent },
];
